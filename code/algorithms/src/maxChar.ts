// Given a string of characters, return the character that appears the most often.

export default function maxChar(sentence: string): string {
 let char = '';

  // remove the white spaces
  // split the string into an array
 // double loop. First to get the letter and the second 
 // to search inside, and count how many times this letter exist. 
 // maybe store in a map: letter - number of times
 
 // loop each letter
 // matchAll(regexp) -> letter --- Store the first item as max... Comnpare
 
 return char;
}